{
	"name": "chatty",
	"private": true,
	"version": "0.2.0-rc.1",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"engines": {
		"node": ">=22.22.2"
	},
	"scripts": {
		"audit:rules": "node scripts/run-audit.mjs",
		"backup:prod": "scripts/backup-production.sh",
		"build": "npm run build --workspaces --if-present",
		"db:down": "docker compose down",
		"db:up": "docker compose up -d postgres",
		"dev:server": "npm run dev --workspace apps/server",
		"dev:web": "npm run dev --workspace apps/web",
		"e2e:install": "playwright install chromium",
		"format": "prettier --write .",
		"format:check": "prettier --check . --end-of-line auto --cache --cache-location=node_modules/.prettier-cache",
		"lint": "eslint . --cache --cache-location node_modules/.eslintcache",
		"restore:prod": "scripts/restore-production.sh",
		"seed:demo": "node scripts/seed-demo.mjs",
		"test": "npm run test --workspaces --if-present",
		"test:changed": "npm run test:changed --workspaces --if-present",
		"test:e2e": "playwright test",
		"typecheck": "npm run typecheck --workspaces --if-present && npm run typecheck:e2e",
		"typecheck:e2e": "tsc -p tsconfig.e2e.json --incremental --tsBuildInfoFile node_modules/.chatty-e2e-typecheck.tsbuildinfo",
		"verify": "npm run version:check && npm run typecheck && npm run lint && npm run format:check && npm run test:changed && npm run verify:audit",
		"verify:full": "npm run version:check && npm run typecheck && npm run lint && npm run format:check && npm run test && npm run verify:audit",
		"verify:audit": "node scripts/run-audit.mjs --gate",
		"version:check": "node scripts/check-version.mjs"
	},
	"devDependencies": {
		"@eslint/js": "^9.15.0",
		"@playwright/test": "^1.62.1",
		"eslint": "^9.15.0",
		"eslint-config-prettier": "^9.1.0",
		"prettier": "^3.3.3",
		"typescript": "^5.6.3",
		"typescript-eslint": "^8.14.0"
	},
	"allowScripts": {
		"@prisma/client@5.22.0": true,
		"@prisma/engines@5.22.0": true,
		"bcrypt@6.0.0": true,
		"esbuild@0.28.2": true,
		"ffmpeg-static@5.3.0": true,
		"prisma@5.22.0": true
	}
}
