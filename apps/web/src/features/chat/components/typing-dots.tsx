/** Three staggered beats share one rhythm across the thread and its navigation. */
export function TypingDots() {
	return (
		<span aria-hidden="true" className="typing-dots inline-flex h-3 shrink-0 items-center gap-[3px]">
			<span />
			<span />
			<span />
		</span>
	);
}
